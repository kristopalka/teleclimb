import { Component } from "react";
import OpenCompetitionWrapper from "../opening_competition/OpenCompetitionWrapper";

class OpenCompetition extends Component {
  render() {
    return (
      <div className={"open-competition"}>
        <OpenCompetitionWrapper />
      </div>
    );
  }
}

export default OpenCompetition;
